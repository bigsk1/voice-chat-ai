# Spark-TTS Additional Dependencies
# Install AFTER requirements.txt
#
# Install with:
#   pip install -r requirements_sparktts.txt

# Spark-TTS specific packages
huggingface-hub>=0.20.0
safetensors>=0.4.0
accelerate>=0.20.0
omegaconf>=2.3.0
hydra-core>=1.3.0
einops>=0.7.0
einx>=0.3.0
soxr>=0.5.0

