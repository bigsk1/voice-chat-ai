FROM python:3.11-slim
WORKDIR /app
COPY requirements-minimal.txt /app/
RUN pip install --no-cache-dir -r requirements-minimal.txt
COPY . /app
EXPOSE 80
CMD ["uvicorn", "minimal_app:app", "--host", "0.0.0.0", "--port", "80"]
